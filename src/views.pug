if view === 'auth'
  include views/auth.pug
else if view === 'dashboard'
  include views/dashboard.pug
else if view === 'create'
  include views/create.pug
else if view === 'detail'
  include views/detail.pug
else if view === 'settings'
  include views/settings.pug
else if view === 'register'
  include views/register.pug